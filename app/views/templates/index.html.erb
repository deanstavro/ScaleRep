<div id="title" class="jumbotron">

    <h1>Templates</h1>

    <% if @user.role == 'scalerep' %>
        <div>
          <%= form_tag({}, {:method => :get}) do %>
            <%= label_tag  'select company:' %>      
                  <%= select_tag :client_company, options_for_select(@client_companies, @company.name), :onchange => "this.form.submit();" %>
          <% end %>
        </div>
    <% end %>

    <div>
      <%= link_to '+ New Template', new_template_path(company_id: @company.id), :class=>"add_campaign btn btn-primary btn-xl page-scroll" %>
    </div>
</div>

<div class="table-display">
  <table class="table table-hover">
    
    <thead class="thead">
      <tr>
        <th scope="col">Created Date</th>
        <th scope="col">Template Title</th>
        <th scope="col">Subject Line</th>
        <th scope="col">Body</th>
        <th scope="col">Edit</th>
        <th scope="col">Delete</th>
      </tr>
    </thead>

    <tbody>
      <% @templates.each do |template| %>
        <tr>

          <td><%= template.created_at.to_time.strftime('%m/%d/%Y') %></td>
          
          <td><%= template.title %></td>

          <td><%= template.subject %></td>

          <td><%= template.body %></td>

          <td><%= link_to 'Edit', edit_template_path(template) %></td>

          <td><%= link_to 'Delete', template, method: :delete, data: { confirm: 'Are you sure?' } %></td>


        </tr>
      <% end %>
    </tbody>
  </table>
  
  <div class="digg_pagination">
      <div class="page_info">
          <%= page_entries_info @templates %>
      </div>
      <%= will_paginate @templates, :params => { :tab_id => :current }, :container => false %>
  </div>
  
</div>
